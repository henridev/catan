export const WIDTH = 1920
export const HEIGHT = 1080

export const X_CENTER = WIDTH / 2
export const Y_CENTER = HEIGHT / 2
export const TILE_RADIUS = 70
export const X_TILE_SPACE = Math.sqrt(3) * TILE_RADIUS // 60 is the TILE_RADIUS
export const Y_TILE_SPACE = (3 / 2) * TILE_RADIUS
